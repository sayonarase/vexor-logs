# Allow the vexor-owned VictoriaLogs to bind privileged syslog ports (e.g. 514)
# even with NoNewPrivileges=yes. Harmless when the syslog receiver is disabled.
[Service]
AmbientCapabilities=CAP_NET_BIND_SERVICE
CapabilityBoundingSet=CAP_NET_BIND_SERVICE
